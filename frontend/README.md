# Frontend

This is a simple static HTML client. To run locally:

python -m http.server 3000
open http://localhost:3000

Edit the WS_URL in index.html to point to your deployed backend when you host it.
